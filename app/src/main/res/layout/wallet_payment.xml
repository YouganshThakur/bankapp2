<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Wallet"
        android:textColor="#000000"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:textSize="17sp"
        android:id="@+id/walletHeading"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:text="Wallet Balance : 1000 Rs."
        android:textColor="#000000"
        app:layout_constraintTop_toBottomOf="@+id/walletHeading"
        android:textSize="17sp"
        android:layout_marginTop="24dp"
        android:id="@+id/walletBalance"/>
    <Button
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:text="Pay now"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/walletBalance"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="48dp"
        android:layout_marginRight="48dp"
        android:background="@drawable/add_money_button"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:id="@+id/walletPaynow"
        android:layout_marginStart="48dp"
        android:layout_marginEnd="48dp" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your wallet should have sufficient\nmoney for payment"
        app:layout_constraintTop_toBottomOf="@+id/walletPaynow"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="12dp"
        android:textSize="16sp"
        android:id="@+id/walletWarning"/>
</android.support.constraint.ConstraintLayout>